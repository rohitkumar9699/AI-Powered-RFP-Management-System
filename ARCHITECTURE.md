# System Architecture & Data Flow

## 🏗️ High-Level System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE (Angular)                    │
│                                                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐ │
│  │ RFP Creator  │  │ Vendor Mgmt  │  │ Proposal Evaluator       │ │
│  │              │  │              │  │                          │ │
│  │ Natural Lang │  │ CRUD Vendor  │  │ Compare Proposals        │ │
│  │ Input        │  │ Contact Info │  │ AI Scoring               │ │
│  │ AI Parsing   │  │ Status Track │  │ Recommendations          │ │
│  └──────┬───────┘  └──────┬───────┘  └──────────┬───────────────┘ │
└─────────┼──────────────────┼────────────────────┼──────────────────┘
          │                  │                    │
          └──────────────────┼────────────────────┘
                             │ HTTP REST API
          ┌──────────────────▼────────────────────┐
          │  Django REST Framework Endpoints     │
          │  (localhost:8000/api/...)             │
          └──────────────────┬────────────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
    ┌────▼─────┐  ┌──────────▼────────┐  ┌──────▼──────────┐
    │ Vendors  │  │    RFPs & Propo- │  │  AI & Email     │
    │ CRUD     │  │    sals          │  │  Services       │
    │ Handler  │  │    Management    │  │                 │
    └────┬─────┘  └──────────┬───────┘  └──────┬──────────┘
         │                   │                   │
    ┌────▼────────────────────▼───────────────────▼────┐
    │          MongoDB Database                       │
    │  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │
    │  │ Vendors  │  │   RFPs   │  │  Proposals   │  │
    │  └──────────┘  └──────────┘  └──────────────┘  │
    └────┬────────────────────────────────────────────┘
         │
    ┌────┴─────────────────────────────────────────────┐
    │  External Services                              │
    │  ┌──────────────────┐    ┌──────────────────┐  │
    │  │ OpenAI GPT-4     │    │ Email (SMTP/IMAP)│  │
    │  │ • RFP parsing    │    │ • Send RFPs      │  │
    │  │ • Proposal parse │    │ • Receive props  │  │
    │  │ • Evaluation     │    │                  │  │
    │  └──────────────────┘    └──────────────────┘  │
    └─────────────────────────────────────────────────┘
```

## 📊 Data Model Relationships

```
┌─────────────────────────────────────────────────────────────┐
│                     VENDOR                                  │
├─────────────────────────────────────────────────────────────┤
│ • id (ObjectId)                                             │
│ • name                                                      │
│ • email                                                     │
│ • contact_person, phone, address, city, country            │
│ • website, notes                                            │
│ • active (boolean)                                          │
│ • created_at, updated_at                                    │
└──────────────────┬──────────────────────────────────────────┘
                   │ 1:N (Vendor sends RFP to many vendors)
                   │
        ┌──────────┴──────────┐
        │                     │
        ▼                     ▼
┌──────────────────────┐  ┌───────────────────────┐
│        RFP           │  │     PROPOSAL          │
├──────────────────────┤  ├───────────────────────┤
│ • id (ObjectId)      │  │ • id (ObjectId)       │
│ • title              │  │ • rfp_id (FK)         │
│ • description        │  │ • vendor_id (FK)      │
│ • requirements (JSON)│  │ • vendor_name         │
│ • budget             │  │ • proposal_content    │
│ • deadline           │  │ • parsed_data (JSON)  │
│ • status             │  │ • price               │
│ • selected_vendors[] │  │ • delivery_time       │
│ • awarded_vendor     │  │ • warranty            │
│ • natural_language.. │  │ • payment_terms       │
│ • created_at         │  │ • score (0-100)       │
│ • updated_at         │  │ • evaluation (JSON)   │
└──────────────────────┘  │ • status              │
                          │ • received_at         │
                          └───────────────────────┘
```

## 🔄 Complete Workflow Flow

```
START
  │
  ▼
┌─────────────────────────────────────────┐
│ 1. PROCUREMENT NEED                     │
│ User describes: "Need 20 laptops,       │
│  16GB RAM, $50K budget, 30-day delivery"│
└────────────┬────────────────────────────┘
             │
             ▼
    ┌─────────────────────┐
    │  Send to AI:        │
    │  parse_natural_     │
    │  language_to_rfp()  │
    └────────┬────────────┘
             │
             ▼
┌────────────────────────────────────────────┐
│ 2. RFP CREATED & STORED                    │
│                                            │
│ ✓ Title: "Laptops and Monitors"            │
│ ✓ Requirements: items, specs, qty          │
│ ✓ Budget: 50000                            │
│ ✓ Deadline: 2024-02-10                     │
│ ✓ Status: DRAFT                            │
└────────────┬─────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────┐
│ 3. SELECT & SEND TO VENDORS                │
│                                            │
│ User selects: [Vendor1, Vendor2, Vendor3] │
│ System generates professional email       │
│ AI creates body: generate_rfp_email_body()│
│ Send via SMTP to each vendor               │
│ Status: SENT                               │
└────────────┬─────────────────────────────┘
             │
       ┌─────┴──────────┬──────────────┐
       │                │              │
       ▼                ▼              ▼
    Vendor1         Vendor2         Vendor3
    receives       receives        receives
    RFP email      RFP email       RFP email
       │                │              │
       └─────────┬──────┴──────────────┘
                 │
              [Wait for responses]
                 │
       ┌─────────▼──────────────┐
       │ Vendor replies with    │
       │ proposal email:        │
       │ - Price: $48,000       │
       │ - Delivery: 15 days    │
       │ - Warranty: 2 years    │
       │ - Terms: Net 30        │
       └────────┬───────────────┘
                │
                ▼
    ┌──────────────────────────┐
    │ 4. CHECK INBOX           │
    │                          │
    │ POST /email/check-       │
    │       proposals/          │
    │                          │
    │ System fetches unseen    │
    │ emails via IMAP          │
    └────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────────────────┐
│ 5. PROPOSALS RECEIVED                    │
│                                          │
│ ✓ Proposal from Vendor1 stored           │
│ ✓ Status: RECEIVED                       │
│ ✓ Raw email content preserved            │
│ ✓ Linked to RFP automatically            │
└────────┬─────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ 6. AI PARSING                            │
│                                          │
│ POST /proposals/{id}/parse/              │
│                                          │
│ Send to AI: parse_proposal()             │
│ Extract:                                 │
│ ✓ Price: 48000                           │
│ ✓ Delivery: 15 days                      │
│ ✓ Warranty: 2 years                      │
│ ✓ Payment Terms: Net 30                  │
│ Status: PARSED                           │
└────────┬─────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ 7. COMPARISON & EVALUATION               │
│                                          │
│ POST /proposals/                         │
│      compare_and_evaluate/               │
│                                          │
│ Input:                                   │
│ - RFP requirements                       │
│ - All proposals (3+)                     │
│                                          │
│ AI evaluation via evaluate_proposals()   │
│ For each proposal:                       │
│ ✓ Compliance Score (95/100)              │
│ ✓ Price Score (90/100)                   │
│ ✓ Delivery Score (92/100)                │
│ ✓ Overall Score (92/100)                 │
│                                          │
│ AI Recommendation:                       │
│ "Vendor1 best value - score 92,          │
│  meets budget, fast delivery"            │
│                                          │
│ Status: EVALUATED                        │
└────────┬─────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ 8. AWARD RFP                             │
│                                          │
│ POST /rfps/{id}/award/                   │
│                                          │
│ User approves AI recommendation          │
│ Award to: Vendor1                        │
│ Status: AWARDED                          │
└────────┬─────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ 9. CLOSE RFP                             │
│                                          │
│ POST /rfps/{id}/close/                   │
│                                          │
│ Status: CLOSED                           │
│ Audit trail complete                     │
│ Ready for contract execution             │
└────────┬─────────────────────────────────┘
         │
         ▼
       END
```

## 🔌 API Interaction Sequence

```
┌─────────┐                    ┌─────────┐                ┌────────┐
│Frontend │                    │ Backend │                │ AI/DB  │
│Angular  │                    │ Django  │                │        │
└────┬────┘                    └────┬────┘                └───┬────┘
     │                              │                        │
     │  POST /rfps/create...        │                        │
     │──────────────────────────────>│                        │
     │     {"description": "..."}    │                        │
     │                              │  parse_natural_        │
     │                              │  language_to_rfp()     │
     │                              │───────────────────────>│
     │                              │                        │
     │                              │  Return: structured    │
     │                              │  RFP (JSON)            │
     │                              │<───────────────────────│
     │   RFP Created (DRAFT)        │                        │
     │<──────────────────────────────│                        │
     │                              │                        │
     │  POST /rfps/{id}/            │                        │
     │       send_to_vendors/       │                        │
     │──────────────────────────────>│                        │
     │   {"vendor_ids": [...]}      │                        │
     │                              │ generate_rfp_          │
     │                              │ email_body()           │
     │                              │───────────────────────>│
     │                              │                        │
     │                              │ Return: email body     │
     │                              │<───────────────────────│
     │                              │                        │
     │                              │ Send via SMTP          │
     │                              │ (external email)       │
     │                              │───────────────────────>│
     │                              │                        │
     │   RFP Sent to Vendors        │                        │
     │<──────────────────────────────│                        │
     │                              │                        │
     │   [Vendors reply via email]  │                        │
     │                              │                        │
     │  POST /email/check-          │                        │
     │       proposals/             │                        │
     │──────────────────────────────>│                        │
     │                              │ Fetch via IMAP         │
     │                              │───────────────────────>│
     │                              │ Return: proposals      │
     │                              │<───────────────────────│
     │   Proposals Imported         │                        │
     │<──────────────────────────────│                        │
     │                              │                        │
     │  POST /proposals/{id}/       │                        │
     │       parse/                 │                        │
     │──────────────────────────────>│                        │
     │                              │ parse_proposal()       │
     │                              │───────────────────────>│
     │                              │ Return: parsed data    │
     │                              │<───────────────────────│
     │   Proposal Parsed            │                        │
     │<──────────────────────────────│                        │
     │                              │                        │
     │  POST /proposals/            │                        │
     │       compare_and_           │                        │
     │       evaluate/              │                        │
     │──────────────────────────────>│                        │
     │   {"rfp_id": "..."}          │                        │
     │                              │ evaluate_proposals()   │
     │                              │───────────────────────>│
     │                              │                        │
     │                              │ Scoring & comparison   │
     │                              │ with AI                │
     │                              │<───────────────────────│
     │   Proposals Evaluated        │                        │
     │   with Scores & Recs         │                        │
     │<──────────────────────────────│                        │
     │                              │                        │
     │  POST /rfps/{id}/award/      │                        │
     │──────────────────────────────>│                        │
     │   {"vendor_id": "..."}       │                        │
     │                              │                        │
     │   RFP Awarded                │                        │
     │<──────────────────────────────│                        │
     │                              │                        │
```

## 📈 Data Flow Through AI

```
┌──────────────────────────────────┐
│ 1. RFP Creation                  │
├──────────────────────────────────┤
│ Input:                           │
│ "I need 20 laptops 16GB RAM,    │
│  $50K budget, 30-day delivery"  │
└────────────┬─────────────────────┘
             │
             ▼
    ┌─────────────────────┐
    │ parse_natural_      │
    │ language_to_rfp()   │
    │                     │
    │ GPT-4-Turbo Call    │
    │ Temperature: 0.3    │
    │ (Factual)           │
    └────────┬────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ Output JSON:                     │
├──────────────────────────────────┤
│ {                                │
│   "title": "...",                │
│   "requirements": {              │
│     "items": [...],              │
│     "delivery": "30 days",       │
│     "budget": 50000              │
│   },                             │
│   "deadline": "2024-02-10"       │
│ }                                │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│ 2. Proposal Parsing              │
├──────────────────────────────────┤
│ Input:                           │
│ "Dear Sir/Madam,                │
│  We propose 20 laptops           │
│  Price: $48,000                  │
│  Delivery: 15 days               │
│  Warranty: 2 years..."           │
└────────────┬─────────────────────┘
             │
             ▼
    ┌─────────────────────┐
    │ parse_proposal()    │
    │                     │
    │ GPT-4-Turbo Call    │
    │ Temperature: 0.2    │
    │ (Conservative)      │
    └────────┬────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ Output JSON:                     │
├──────────────────────────────────┤
│ {                                │
│   "price": 48000,                │
│   "delivery_time": "15 days",    │
│   "warranty": "2 years",         │
│   "payment_terms": "Net 30"      │
│ }                                │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│ 3. Proposal Evaluation           │
├──────────────────────────────────┤
│ Input:                           │
│ - RFP requirements               │
│ - 3 vendor proposals (parsed)    │
└────────────┬─────────────────────┘
             │
             ▼
    ┌─────────────────────┐
    │ evaluate_           │
    │ proposals()         │
    │                     │
    │ GPT-4-Turbo Call    │
    │ Temperature: 0.3    │
    │ (Analytical)        │
    └────────┬────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ Output JSON:                     │
├──────────────────────────────────┤
│ {                                │
│   "evaluations": {               │
│     "Vendor1": {                 │
│       "score": 92,               │
│       "compliance": 95,          │
│       "price_comp": 90,          │
│       "risk": "Low"              │
│     },                           │
│     ...                          │
│   },                             │
│   "recommendation": "Vendor1..." │
│ }                                │
└──────────────────────────────────┘
```

## 🔐 Security Flow

```
Frontend (4200)
     │
     │ CORS Check
     ├────────────────────┐
     │                    │
     ▼                    ▼
  ALLOWED            BLOCKED
  (localhost:4200)   (other origins)
     │
     ▼
Backend API (8000)
     │
     ├─ Input Validation (Serializers)
     ├─ Error Handling (try-catch)
     ├─ Rate Limiting (headers)
     │
     ▼
Database (MongoDB)
     │
     ├─ ORM Protection (Djongo)
     ├─ Connection Pooling
     ├─ SSL/TLS Ready
     │
     ▼
External APIs
     │
     ├─ OpenAI (API Key in ENV)
     ├─ Email SMTP/IMAP (Credentials in ENV)
     │
     ▼
No secrets in code / Git
No passwords hardcoded
All in environment variables
```

---

This architecture ensures:
✅ Clear separation of concerns
✅ AI integration at key decision points
✅ Email automation end-to-end
✅ Data consistency and integrity
✅ Scalability and extensibility
✅ Production-ready security
